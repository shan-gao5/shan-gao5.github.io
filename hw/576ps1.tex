\documentclass[12pt]{article}



\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}


\begin{document}
\begin{enumerate}
	\item 
		\begin{itemize}
			\item The subspace topology of $[0,1] \subseteq \mathbb{R}$ with the usual topology of $\mathbb{R}$ has open sets generated by unions of these open intervals in $[0,1]$
				$$G = \{[0,a), (b,1], (c,d)\colon 0<a\leq 1, 0 \leq b < 1, 0\leq c\leq d \leq 1\}$$, it is not possible to cover $[0,1]$ from a disjoint union of two sets generated by unions of any combination of the above. Suppose it's true, then there exists open sets $U,V$ generated by unions of intervals in $G$, where $U \cap V = \emptyset$ and $U \cup V = [0,1]$. If $U$ is generated by intervals of the form $[0,a)$ (let $a$ be the largest), then $V$ must only contain intervals of the form $(b,1] $ and $(c,d)$ and it must contain the complementary interval $[a,1]$, but that's impossible to generated. If $U$ contains an interval of the form $(b,1]$, then the argument is similar, where $V$ must contain the complementary interval $[0,b]$ that cannot be generatefrom intervals of the form $[0,a)$ and $(c,d)$. If $U$ is generated by intervals of the form $(b,1]$ and $[0,a)$, then $V$ must contain the complementary interval $[a,b]$ which cannot be generated by open intervals $(a,b)$. If $U$ contains all three types of intervals, $V$ is only allowed intervals of the form $(a,b)$, which cannot cover $[0,1]$. If $U$ is generated by intervals of the type $[0,a)$ and $(a,b)$
		\end{itemize}
\end{enumerate}

\end{document}
